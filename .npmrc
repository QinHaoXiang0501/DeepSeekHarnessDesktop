# 国内镜像：GitHub releases 直连不通时，用 npmmirror 加速 Electron 与 electron-builder 二进制下载。
# 这些键会被 npm 注入为 npm_config_* 环境变量，@electron/get 与 electron-builder 会读取。
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
